const { Box } = require("@mui/material");
import { styled } from '@mui/material/styles';

const FlexEndBox = styled(Box)({
  display: "flex",
  justifyContent: "flex-end",
  alignItems: "center",
});

export default FlexEndBox;